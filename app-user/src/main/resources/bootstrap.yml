#server:
#  port: 9091

spring:
  application:
    name: app-user
  cloud:
    config:
      label: master
      profile: dev
      discovery:
        enabled: true
        service-id: infra-config
      username: admin
      password: admin123
#      uri: http://admin:admin123@localhost:8888/
  zipkin:
    base-url: http://localhost:9411

